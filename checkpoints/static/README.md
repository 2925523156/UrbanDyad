# Static Encoder Checkpoint

This directory contains the released UrbanDyad static representation checkpoint.

```text
urban_dyad_static_encoder.ckpt
config.json
```

The checkpoint should be uploaded with Git LFS because it is larger than the normal
GitHub file limit. Raw data, dynamic checkpoints, downstream predictor weights, and
learned embedding arrays are not included in this repository.
